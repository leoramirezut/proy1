<%= form_with(model: ap, local: true) do |form| %>
  <% if ap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ap.errors.count, "error") %> prohibited this ap from being saved:</h2>

      <ul>
      <% ap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :ap_nombre %>
  </div>

  <div class="field">
    <%= form.label :edad %>
    <%= form.number_field :edad, id: :ap_edad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
